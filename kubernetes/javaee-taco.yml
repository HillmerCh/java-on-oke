apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: javaee-taco
  namespace: default
spec:
  replicas: 2
  template:
    metadata:
      name: javaee-taco
      labels:
        app: javaee-taco
    spec:
      containers:
      - name: javaee-taco
        env:
          - name: POSTGRES_USER
            valueFrom:
              configMapKeyRef:
                name: postgres-config
                key: postgres_user
          - name: POSTGRES_PASSWORD
            valueFrom:
              configMapKeyRef:
                name: postgres-config
                key: postgres_password
          - name: POSTGRES_HOST
            valueFrom:
              configMapKeyRef:
                name: hostname-config
                key: postgres_host
        image: hillmerch/javaee-taco:v6
        imagePullPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: javaee-taco
spec:
  type: LoadBalancer
  ports:
    - port: 9080
  selector:
    app: javaee-taco